[package]
name = "compute-starter-kit-rust-default"
version = "0.1.0"
authors = []
edition = "2018"

[profile.release]
debug = true

[dependencies]
fastly = "^0.6.0"
serde = { version = "1.0", features = [ "derive" ] }
tinytemplate = "^1.2"
